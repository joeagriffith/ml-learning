
def eval(
    model,
    data_loader,
    criterion,

):

def train_ff(
        model,
        train_loader,
        val_loader,
        mode='default'
        eval_criterion,


):
    modes = ['default', 'pc']
    assert mode in modes, f"mode: {mode} is invalid, must be one of: {modes}"



